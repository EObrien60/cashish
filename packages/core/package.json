{
  "name": "@cashish/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The schema, the connection, the tenant context and the permission policy — shared by every application.",
  "exports": {
    "./db": "./src/db/index.ts",
    "./rbac": "./src/rbac.ts",
    "./migrate": "./src/migrate.ts"
  },
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx scripts/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.4",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "drizzle-kit": "^0.30.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
